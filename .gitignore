# Office lock + temp files
~$*
*.tmp

# OS / sync artifacts
Thumbs.db
desktop.ini
.DS_Store

# Credentials must never enter version control
*Logins*
*ogins*.xlsx
*password*
*credential*

# Claude CRM is its own separate git repo — excluded from the Marketing repo
Claude CRM/
